#!/bin/bash
NO_SSH="false"
LOCAL_MACHINE=`echo ${SSH_CLIENT}|cut -f 1 -d " "`
LOCAL_NET=`echo ${LOCAL_MACHINE}|cut -f 1,2 -d .`
ssh -x ${USER}@${LOCAL_MACHINE} DISPLAY=:0.0 ${HOME}/usr/bin/task_notification.local "${HOSTNAME}:${PWD}"

