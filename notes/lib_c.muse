* LIBC

** fcntl

- FD_CLOEXEC is used to set close-on-exec flag of a fd. 

---+ System V IPC

%ICON{note}% System V IPC is not a recommended sync method.

   * signal queue -&gt; stream
<blockquote> but seems no stream in Linux. the libc info page only lists pipe, FIFO and socket. *Use FIFO instead of signal queue* </blockquote>

   * semaphore -&gt; file lock

   * shm -&gt; mmap /dev/zero
